/** @type {import('next').NextConfig} */
// const withTM = require('next-transpile-modules')(['bcryptjs', 'dotenv'])
const nextConfig = {
  reactStrictMode: true,
}
module.exports = nextConfig
// module.exports = withTM(nextConfig)
